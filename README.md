# Six Morals

Text classifier for Jonathan Haidt's six moral foundations

## Training

Example execution of model training

```bash
uv run python training.py --dataset-path dataset.csv --model-name "distilbert/distilbert-base-uncased" --output-path "my_awesome_model"
```
